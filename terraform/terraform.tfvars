# 🔹 AWS Account ID
aws_account_id = "891377332738"

# 🔹 AWS Region
aws_region = "us-east-1"

# 🔹 AWS Profile (El perfil de AWS CLI que usaste)
aws_profile = "default"

# 🔹 Database Credentials (Asegúrate de que coincidan con `variables.tf`)
db_username = "admin"
db_password = "1014Franlondon@@" # ⚠️ Cambia esto por una contraseña segura

# 🔹 S3 Bucket Name for Terraform State
s3_bucket_name = "terraform-state-frank"

# 🔹 DynamoDB Table for State Locking
dynamodb_table_name = "terraform-locks"
